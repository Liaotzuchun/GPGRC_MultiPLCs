<UserControl x:Class="GPGO_MultiPLCs.Views.TotalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:local="clr-namespace:GPGO_MultiPLCs.Views"
             xmlns:m="clr-namespace:GPGO_MultiPLCs.Models"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:GPGO_MultiPLCs.ViewModels"
             d:DataContext="{d:DesignInstance vm:TotalView_ViewModel}"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">
    <Grid>
        <TabControl SelectedIndex="{Binding Index, Mode=TwoWay}">
            <TabItem Width="0"
                     Height="0">
                <ListBox ItemsSource="{Binding PLC_In_All, Mode=OneTime}"
                         SelectedIndex="{Binding ViewIndex, Mode=TwoWay}">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="1" />
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemTemplate>
                        <DataTemplate DataType="m:PLC_Data">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Margin="8"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           Text="{Binding StationNumber, Mode=OneWay, StringFormat=第 \{0:D\} 站}" />
                                <TextBlock HorizontalAlignment="Center"
                                           VerticalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding OnlineStatus, Mode=OneWay}"
                                                             Value="true">
                                                    <Setter Property="Text" Value="線上" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding OnlineStatus, Mode=OneWay}"
                                                             Value="false">
                                                    <Setter Property="Text" Value="離線" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </TabItem>
            <TabItem Width="0"
                     Height="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="40" />
                        <RowDefinition Height="40" />
                        <RowDefinition />
                        <RowDefinition Height="40" />
                    </Grid.RowDefinitions>
                    <Button Content="返回主頁">
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="Click">
                                <i:InvokeCommandAction Command="{Binding BackCommand, Mode=OneTime}"
                                                       CommandParameter="0" />
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </Button>
                    <ComboBox Grid.Row="1"
                              HorizontalContentAlignment="Center"
                              VerticalContentAlignment="Center"
                              ItemsSource="{Binding PLC_In_All, Mode=OneTime}"
                              SelectedIndex="{Binding ViewIndex, Mode=TwoWay}">
                        <ComboBox.ItemTemplate>
                            <DataTemplate DataType="m:PLC_Data">
                                <TextBlock Text="{Binding StationNumber, Mode=OneWay, StringFormat=第 \{0:D\} 站}" />
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                    <Grid Grid.Row="2"
                          DataContext="{Binding PLC_In_Focused, Mode=OneWay}" />
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
