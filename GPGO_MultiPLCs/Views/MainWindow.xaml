<Window x:Class="GPGO_MultiPLCs.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:gpmodels="clr-namespace:GPMVVM.Models;assembly=GPMVVM"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:local="clr-namespace:GPGO_MultiPLCs.Views"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:GPMVVM.Views;assembly=GPMVVM"
        xmlns:GPSECS="clr-namespace:GP_SECS_GEM;assembly=GP_SECS_GEM"
        xmlns:GPSECSUI="clr-namespace:GP_SECS_GEM.UIControl;assembly=GP_SECS_GEM"
        x:Name="Main"
        MinWidth="1024"
        MinHeight="768"
        DataContext="{Binding MainVM, Source={StaticResource Connector}, Mode=OneTime}"
        Icon="/GPGO_MultiPLCs;component/GP.ico"
        Loaded="Window_Loaded"
        Style="{StaticResource UniversalWindow}"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        mc:Ignorable="d">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Closing">
            <gpmodels:InteractiveCommand Command="{Binding ClosingCommand, Mode=OneTime}" />
        </i:EventTrigger>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadedCommand, Mode=OneTime}"
                                   CommandParameter="{Binding Dispatcher, ElementName=Main}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="224" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="78" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid x:Name="PopGrid"
              Grid.RowSpan="2"
              Grid.ColumnSpan="2">
            <Border Background="#7F333333">
                <Border.Visibility>
                    <MultiBinding Converter="{StaticResource MultiAnyVisible}">
                        <Binding ElementName="Authenticator"
                                 Mode="OneWay"
                                 Path="Visibility" />
                        <Binding ElementName="GlobalDialog"
                                 Mode="OneWay"
                                 Path="Visibility" />
                    </MultiBinding>
                </Border.Visibility>
            </Border>
            <Grid>
                <views:ScreenKeyboard UILanguage="{Binding Language, Source={StaticResource Connector}, Mode=OneWay}" />
                <local:Authenticator x:Name="Authenticator"
                                     DataContext="{StaticResource AuthenticatorViewModel}" />
                <views:GlobalDialog x:Name="GlobalDialog"
                                    DataContext="{Binding DialogVM, Source={StaticResource Connector}, Mode=OneTime}"
                                    IsVisibleChanged="GlobalDialog_IsVisibleChanged" />
            </Grid>
        </Grid>
        <Grid Background="{StaticResource LightColorBrush}">
            <ListBox FontSize="20"
                     ItemContainerStyle="{StaticResource ListBoxItem_Top}"
                     SelectedIndex="{Binding ViewIndex, Mode=TwoWay}">
                <ListBox.Template>
                    <ControlTemplate>
                        <ItemsPresenter />
                    </ControlTemplate>
                </ListBox.Template>
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <views:UniformStackPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <TextBlock Text="{DynamicResource 生產頁面}" />
                <TextBlock Text="{DynamicResource 配方管理}" />
                <TextBlock Text="{DynamicResource 生產履歷}" />
                <TextBlock Text="{DynamicResource 事件履歷}" />
                <!--<TextBlock Text="{DynamicResource GEMCongig}" />
                <TextBlock Text="{DynamicResource GEMData}" />-->
            </ListBox>
        </Grid>
        <Border Grid.Column="1"
                Background="{StaticResource LightColorBrush}"
                Child="{StaticResource GP_Logo}" />
        <TabControl Grid.Row="1"
                    Grid.ColumnSpan="2"
                    views:TabControl.EnableSliding="True"
                    Background="{StaticResource WindowBackgroundBrush}"
                    SelectedIndex="{Binding ViewIndex, Mode=OneWay}">
            <TabItem Width="0"
                     Height="0">
                <local:TotalView DataContext="{Binding TotalVM, Source={StaticResource Connector}, Mode=OneWay}" />
            </TabItem>
            <TabItem Width="0"
                     Height="0">
                <local:RecipeControl Margin="0,4,0,0"
                                     DataContext="{Binding RecipeVM, Source={StaticResource Connector}, Mode=OneWay}" />
            </TabItem>
            <TabItem Width="0"
                     Height="0">
                <local:TraceabilityView Margin="0,4,0,0"
                                        DataContext="{Binding TraceVM, Source={StaticResource Connector}, Mode=OneWay}" />
            </TabItem>
            <TabItem Width="0"
                     Height="0">
                <local:LogView Margin="0,4,0,0"
                               DataContext="{Binding LogVM, Source={StaticResource Connector}, Mode=OneWay}" />
            </TabItem>
            <!--<TabItem Width="0" Height="0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <GPSECSUI:SECSParameterView DataContext="{Binding SECSParameterSet}" LanguagesControl="EN">
                        --><!--<GPSECSUI:SECSParameterView.DataContext>
                                <GPSECS:SECSParameterSet />
                            </GPSECSUI:SECSParameterView.DataContext>--><!--
                    </GPSECSUI:SECSParameterView>
                    <GPSECSUI:GemSetView Grid.Column="1" DataContext="{Binding SECS_GEM.GemSetViewModel}" />
                </Grid>
            </TabItem>-->
            <TabItem Width="0" Height="0">
                <GPSECSUI:MutiEqpView>
                    <GPSECSUI:MutiEqpView.DataContext>
                        <GPSECS:MutiEqpViewModel />
                    </GPSECSUI:MutiEqpView.DataContext>
                </GPSECSUI:MutiEqpView>
            </TabItem>
        </TabControl>
    </Grid>
</Window>