<UserControl x:Class="GPGO_MultiPLCs.Views.GlobalDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:local="clr-namespace:GPGO_MultiPLCs.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:GPGO_MultiPLCs.ViewModels"
             d:DataContext="{d:DesignInstance vm:GlobalDialog_ViewModel}"
             Visibility="{Binding IsShown, Mode=OneWay}"
             mc:Ignorable="d">

    <TabControl>
        <TabItem Width="0"
                 Height="0">
            <Border Background="#7FFFFFFF">
                <Border Width="400"
                        Height="200"
                        Background="White"
                        BorderBrush="Gray"
                        BorderThickness="1">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="40" />
                            <RowDefinition />
                        </Grid.RowDefinitions>
                        <Border Background="Gold"
                                BorderBrush="Gray"
                                BorderThickness="0,0,0,1">
                            <TextBlock HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       FontSize="20"
                                       Text="警告!" />
                        </Border>
                        <Grid Grid.Row="1">
                            <Grid.RowDefinitions>
                                <RowDefinition />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Viewbox Margin="20,8,20,4">
                                <TextBlock Text="{Binding Message, Mode=OneWay}"
                                           TextAlignment="Center"
                                           TextWrapping="WrapWithOverflow" />
                            </Viewbox>
                            <StackPanel Grid.Row="1"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Orientation="Horizontal"
                                        TextElement.FontSize="20">
                                <Button Margin="4,8"
                                        Padding="12,8"
                                        Content="確定">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Click">
                                            <i:InvokeCommandAction Command="{Binding OkayCommand, Mode=OneTime}" />
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </Button>
                                <Button Margin="4,8"
                                        Padding="12,8"
                                        Content="取消">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Click">
                                            <i:InvokeCommandAction Command="{Binding CancelCommand, Mode=OneTime}" />
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Border>
            </Border>
        </TabItem>
    </TabControl>
</UserControl>